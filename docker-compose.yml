services:
  webhook:
    container_name: "webhook"
    build:
      context: .
      args:
        DOCKER_GID: 1002

    ports:
      - "12000:80"

    volumes:
      - .:/var/www/html
      - /var/run/docker.sock:/var/run/docker.sock

    restart: unless-stopped
