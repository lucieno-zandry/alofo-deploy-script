services:
  webhook:
    image: "lucienozandry/alofo-docker-webhook:maboo"
    container_name: "webhook"
    build:
      context: .
      args:
        DOCKER_GID: 1002

    ports:
      - "12000:80"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

    restart: unless-stopped
